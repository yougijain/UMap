Django==3.2.9                 # Replace with the Django version you're using
gunicorn==20.1.0              # Production WSGI HTTP server
psycopg2-binary==2.9.1        # PostgreSQL adapter, if using PostgreSQL
whitenoise==5.3.0             # Simplifies static file serving in production
djangorestframework==3.12.4   # If using Django REST Framework for APIs
